# GitHub Pages 是純靜態站，沒有 Vite dev proxy 也沒有部署 server/，
# 所以正式環境只能改打「本身有開放 CORS」的公開資料來源。
#
# data.moenv.gov.tw 有回 Access-Control-Allow-Origin: *，可以直接從瀏覽器呼叫。
#
# 台電的 service.taipower.com.tw／www.taipower.com.tw 兩個端點經實測完全沒有回
# Access-Control-Allow-Origin，瀏覽器一定會擋（不是能靠改網址解決的），因此正式環境
# 沒有加對應的 VITE_TAIPOWER_* 變數——這兩個資料來源目前仍需透過 server/ 代理才能用，
# 詳見 client/CLAUDE.md 與 server/src/taipowerProxy.ts。
VITE_MOENV_BASE_URL=https://data.moenv.gov.tw
