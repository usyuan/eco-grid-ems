# GitHub Pages 是純靜態站，沒有 Vite dev proxy，所以正式環境只有兩種資料來源可用：
# 本身有開放 CORS 的公開 API，或部署在 Cloud Run 上的 server/。
#
# data.moenv.gov.tw 有回 Access-Control-Allow-Origin: *，可以直接從瀏覽器呼叫。
#
# 台電兩支資料（service.taipower.com.tw 的供需摘要與機組出力明細）經實測完全沒有回
# Access-Control-Allow-Origin，瀏覽器一定會擋，因此這裡沒有加對應的 VITE_TAIPOWER_* 變數——
# 兩支都走 server/ 代抓（VITE_API_BASE_URL）。
# 詳見 client/CLAUDE.md 與 server/src/taipowerProxy.ts。
VITE_MOENV_BASE_URL=https://data.moenv.gov.tw

# VITE_SOCKET_URL 與 VITE_API_BASE_URL 刻意不寫在這裡，改由 CI 從 repo variable
# VITE_SERVER_URL 注入（見 .github/workflows/deploy-client-pages.yml）——值是 Cloud Run
# 每次建立服務才決定的 *.run.app 網址，不該硬寫進版控。
# 沒設定時前端會退回 http://localhost:4000，線上即時資料就不會更新（其他頁面照常）。
